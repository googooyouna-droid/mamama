# 0단계: 사전 설정 가이드

## 🎯 목표
- Flutter SDK 설치 및 환경 설정
- Firebase 프로젝트 생성 및 서비스 활성화
- 개발 환경 검증

## 📋 체크리스트

### ✅ 1. Flutter SDK 설치 (Windows)

#### 방법 1: Flutter 공식 설치
1. [Flutter 공식 사이트](https://flutter.dev/docs/get-started/install/windows) 방문
2. Flutter SDK 다운로드 (ZIP 파일)
3. 압축 해제 후 `C:\flutter`에 설치
4. 환경 변수 설정:
   - 시스템 환경 변수 → Path에 `C:\flutter\bin` 추가
   - 새 터미널에서 `flutter doctor` 실행

#### 방법 2: Chocolatey 사용 (권장)
```powershell
# 관리자 권한으로 PowerShell 실행
choco install flutter
```

#### 방법 3: Git Bash 사용
```bash
# Git이 설치되어 있다면
git clone https://github.com/flutter/flutter.git -b stable
# 환경변수 PATH에 flutter/bin 추가
```

### ✅ 2. Flutter 환경 검증
```bash
flutter doctor
```
다음 항목들이 모두 체크되어야 함:
- ✅ Flutter (Channel stable, version)
- ✅ Android toolchain (Android SDK, Android Studio)
- ✅ Visual Studio (Windows)
- ✅ Android Studio
- ✅ VS Code (선택사항)

### ✅ 3. Firebase 프로젝트 생성

#### 3-1. Firebase 콘솔 접속
1. [Firebase 콘솔](https://console.firebase.google.com/) 접속
2. "프로젝트 만들기" 클릭
3. 프로젝트 이름: `mind-diary-app`
4. Google Analytics 활성화 (선택사항)

#### 3-2. 서비스 활성화
1. **Authentication** 활성화
   - Authentication → Sign-in method → 익명 로그인 활성화
   
2. **Firestore Database** 활성화
   - Firestore Database → 데이터베이스 만들기 → 테스트 모드로 시작
   
3. **Storage** 활성화
   - Storage → 시작하기 → 테스트 모드로 시작
   
4. **Hosting** 활성화
   - Hosting → 시작하기

#### 3-3. Firebase CLI 설치
```bash
npm install -g firebase-tools
firebase login
```

### ✅ 4. 프로젝트 설정 정보

#### 앱 기본 정보
- **앱 이름**: mind_diary
- **패키지명**: com.minddiary.app
- **플랫폼**: Flutter Web → Android
- **Firebase 프로젝트 ID**: mind-diary-app

#### Firebase 설정 파일
- Web용: `firebase_options.dart` (자동 생성됨)
- Android용: `google-services.json` (자동 생성됨)

## 🔧 설치 후 확인 명령어

```bash
# Flutter 버전 확인
flutter --version

# Flutter 환경 검증
flutter doctor

# Firebase CLI 확인
firebase --version

# Flutter 프로젝트 생성 테스트
flutter create test_app
cd test_app
flutter run -d web
```

## 📁 생성될 파일/폴더

### Flutter 관련
- `C:\flutter\` (Flutter SDK)
- 환경변수 PATH 설정

### Firebase 관련
- Firebase 콘솔 프로젝트: `mind-diary-app`
- Firebase CLI 설정 완료

### 프로젝트 관련
- `firebase_options.dart` (1단계에서 생성)
- `google-services.json` (1단계에서 생성)

## ⚠️ 주의사항

1. **관리자 권한**: Flutter 설치 시 관리자 권한 필요
2. **인터넷 연결**: Firebase 서비스 활성화 시 인터넷 필요
3. **브라우저**: Chrome 브라우저 권장 (웹 테스트용)
4. **용량**: Flutter SDK 약 2GB 필요

## 🚀 다음 단계
모든 체크리스트가 완료되면 **1번 프롬프트**를 실행하세요!

---

**📅 작성일**: 2025년 1월 16일  
**👤 작성자**: 파이썬 기초 수업 학생  
**📝 버전**: v1.0


